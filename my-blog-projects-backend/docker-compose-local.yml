services:
  my-blog-projects-backend:
    build:
      context: .
      dockerfile: Dockerfile.local
    ports:
      - "1337:1337"
    environment:
      BROWSER: "none"
      SHELL: "/bin/bash"
      NODE_ENV: "local"