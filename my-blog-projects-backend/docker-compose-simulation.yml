services:
  my-blog-projects-backend:
    build:
      context: .
      dockerfile: Dockerfile.simulation
    volumes:
      - ./public/uploads:/app/public/uploads
      - ./.tmp:/app/.tmp
    ports:
      - "1337:1337"
    environment:
      BROWSER: "none"
      SHELL: "/bin/bash"
      NODE_ENV: "simulation"